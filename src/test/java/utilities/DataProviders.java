package utilities;import org.testng.annotations.DataProvider;import org.testng.annotations.Test;import java.io.IOException;public class DataProviders {    //Data Provider 1    @DataProvider (name = "LoginData")    public Object[][] getData() throws IOException {        String path = ".\\testData\\OpenCart_loginData.xlsx";   //take the xls file from test data folder        ExcelUtility xlutil = new ExcelUtility(path);        int totalrows = xlutil.getRowCount("Sheet1");        int totalcolums = xlutil.getCellCount("Sheet1", 1);        String[][] logindata = new String[totalrows][totalcolums];        for (int i = 1; i <= totalrows; i++) {            for (int j = 0; j < totalcolums; j++) {                logindata[i - 1][j] = xlutil.getCellData("Sheet1", i, j);            }        }        System.out.println(totalrows+" "+ totalcolums);        return logindata;      }@Test    public void getData1() throws IOException {        String path = ".\\testData\\OpenCart_loginData.xlsx";   //take the xls file from test data folder        ExcelUtility xlutil = new ExcelUtility(path);        int totalrows = xlutil.getRowCount("Sheet1");        //我一共有6行,为什么lastROWNUMBER返回的是5?        int totalcolums = xlutil.getCellCount("Sheet1", 1);        String[][] logindata = new String[totalrows][totalcolums];        for (int i = 1; i <= totalrows; i++) {            for (int j = 0; j < totalcolums; j++) {                logindata[i - 1][j] = xlutil.getCellData("Sheet1", i, j);            }        }        System.out.println(totalrows+" "+ totalcolums); //返回5,5//        return logindata;    }}